package net.meanscript.java;
import net.meanscript.MSText;

public class MSTextComparator implements java.util.Comparator<MSText> {
	@Override
	public int compare(MSText a, MSText b) {
		return a.compare(b);
	}

}
